\input{references_config.tex}

% Create a reference and set its value
\newcommand{\createref}[2]{
  \expandafter\def\csname ref-#1\endcsname{} % Define the reference symbol
  \def\tmphtml{html}%
  \def\tmppdf{pdf}%
  \ifx\pdfhtmlmode\tmphtml
    \expandafter\def\csname refval-#1\endcsname{\htmlprefix#2.html} % Set the reference value for HTML mode
  \else\ifx\pdfhtmlmode\tmppdf
    \expandafter\def\csname refval-#1\endcsname{\reporoot#2.pdf} % Set the reference value for PDF mode
  \else
    \PackageError{reference-lib}{pdfhtmlmode has an invalid value: \pdfhtmlmode}{Compilation stopped due to a critical error.}
    \stop % Stop compilation
  \fi\fi
}

% Get the value of a reference
\makeatletter
\newcommand{\getref}[1]{%
  \expandafter\@firstofone\expandafter{\csname refval-#1\endcsname}%
}
\makeatother

\newcommand{\createextref}[2]{%
  \expandafter\def\csname erefurl-#1\endcsname{#2}%
}

\newcommand{\getextref}[1]{\csname erefurl-#1\endcsname}

% Helper to print an external reference as a clickable link or \url
\makeatletter
\newcommand{\printextref}[1]{%
  \expandafter\ifx\csname erefurl-#1\endcsname\relax
    \PackageWarning{reference-lib}{External reference `#1' undefined}%
  \else
    \begingroup
      \edef\@tempa{\csname erefurl-#1\endcsname}%
      \@ifundefined{href}{%
        \@ifundefined{url}{%
          \@tempa
        }{%
          \url{\@tempa}%
        }%
      }{%
        \href{\@tempa}{\@tempa}%
      }%
    \endgroup
  \fi
}
\makeatother

\input{references.tex}



